export class Profile {
  id: number;
  user: string;
  // tslint:disable-next-line:variable-name
  first_name: string;
  // tslint:disable-next-line:variable-name
  last_name: string;
  picture?: string;
  pais: string;
  morada: string;
  zipcode: string;
}
